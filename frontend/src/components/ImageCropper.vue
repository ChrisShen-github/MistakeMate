<script setup lang="ts">
import { computed, nextTick, onBeforeUnmount, onMounted, ref } from 'vue'
import { Check, Hand, Maximize2, X, ZoomIn, ZoomOut } from '@lucide/vue'

export type CropRegion = { x: number; y: number; width: number; height: number }
const props = defineProps<{ file: File; initialRegion: CropRegion | null }>()
const emit = defineEmits<{ cancel: []; confirm: [region: CropRegion | null] }>()
const canvas = ref<HTMLCanvasElement | null>(null); const frame = ref<HTMLDivElement | null>(null); const dialog = ref<HTMLElement | null>(null)
const isReady = ref(false); const loadError = ref(''); const zoom = ref(1); const isPanning = ref(false)
const region = ref<CropRegion>(props.initialRegion ? { ...props.initialRegion } : { x: .05, y: .05, width: .9, height: .9 })
let image: HTMLImageElement | null = null; let objectUrl = ''; let displayWidth = 0; let displayHeight = 0; let baseScale = 1; let pan = { x: 0, y: 0 }
let activePointer: number | null = null; let dragMode = ''; let dragOrigin = { x: 0, y: 0 }; let dragRawOrigin = { x: 0, y: 0 }; let dragStart: CropRegion = { ...region.value }; let panStart = { ...pan }; let previousBodyOverflow = ''
const pointers = new Map<number, { x: number; y: number }>(); let pinchDistance = 0
const cropSummary = computed(() => { const { x, y, width, height } = region.value; return `左侧 ${Math.round(x * 100)}%，顶部 ${Math.round(y * 100)}%，宽 ${Math.round(width * 100)}%，高 ${Math.round(height * 100)}%` })
function clamp(value: number, minimum: number, maximum: number) { return Math.min(maximum, Math.max(minimum, value)) }
function isWholeImage(value: CropRegion) { return value.x < .002 && value.y < .002 && value.width > .998 && value.height > .998 }
function imageBox() { const scale = baseScale * zoom.value; const width = image ? image.naturalWidth * scale : 0; const height = image ? image.naturalHeight * scale : 0; return { x: (displayWidth - width) / 2 + pan.x, y: (displayHeight - height) / 2 + pan.y, width, height } }
function clampPan() { const box = imageBox(); pan.x = clamp(pan.x, -Math.max(0, (box.width - displayWidth) / 2), Math.max(0, (box.width - displayWidth) / 2)); pan.y = clamp(pan.y, -Math.max(0, (box.height - displayHeight) / 2), Math.max(0, (box.height - displayHeight) / 2)) }
function draw() {
  const target = canvas.value; if (!target || !image || !isReady.value || !displayWidth || !displayHeight) return
  const ratio = window.devicePixelRatio || 1; const context = target.getContext('2d'); if (!context) return
  context.setTransform(ratio, 0, 0, ratio, 0, 0); context.clearRect(0, 0, displayWidth, displayHeight)
  const imageBounds = imageBox(); context.drawImage(image, imageBounds.x, imageBounds.y, imageBounds.width, imageBounds.height)
  const box = { x: imageBounds.x + region.value.x * imageBounds.width, y: imageBounds.y + region.value.y * imageBounds.height, width: region.value.width * imageBounds.width, height: region.value.height * imageBounds.height }
  context.fillStyle = 'rgba(14,28,45,.62)'; context.fillRect(imageBounds.x, imageBounds.y, imageBounds.width, Math.max(0, box.y - imageBounds.y)); context.fillRect(imageBounds.x, box.y, Math.max(0, box.x - imageBounds.x), box.height); context.fillRect(box.x + box.width, box.y, Math.max(0, imageBounds.x + imageBounds.width - box.x - box.width), box.height); context.fillRect(imageBounds.x, box.y + box.height, imageBounds.width, Math.max(0, imageBounds.y + imageBounds.height - box.y - box.height))
  context.save(); context.strokeStyle = 'rgba(255,255,255,.58)'; context.lineWidth = 1; context.setLineDash([5, 5]); for (const fraction of [1 / 3, 2 / 3]) { context.beginPath(); context.moveTo(box.x + box.width * fraction, box.y); context.lineTo(box.x + box.width * fraction, box.y + box.height); context.stroke(); context.beginPath(); context.moveTo(box.x, box.y + box.height * fraction); context.lineTo(box.x + box.width, box.y + box.height * fraction); context.stroke() }; context.restore()
  context.strokeStyle = '#f97316'; context.lineWidth = 3; context.strokeRect(box.x + 1.5, box.y + 1.5, Math.max(0, box.width - 3), Math.max(0, box.height - 3)); context.fillStyle = '#fff'; context.strokeStyle = '#f97316'; context.lineWidth = 2
  for (const [x, y] of [[box.x, box.y], [box.x + box.width / 2, box.y], [box.x + box.width, box.y], [box.x, box.y + box.height / 2], [box.x + box.width, box.y + box.height / 2], [box.x, box.y + box.height], [box.x + box.width / 2, box.y + box.height], [box.x + box.width, box.y + box.height]]) { context.beginPath(); context.rect(x - 6, y - 6, 12, 12); context.fill(); context.stroke() }
}
function resizeCanvas() { if (!frame.value || !canvas.value || !image) return; const mobile = window.innerWidth <= 680; displayWidth = Math.max(1, Math.floor(frame.value.clientWidth - (mobile ? 12 : 24))); displayHeight = Math.max(220, Math.floor(mobile ? Math.min(window.innerHeight * .42, 390) : Math.min(window.innerHeight - 330, 560))); baseScale = Math.min(displayWidth / image.naturalWidth, displayHeight / image.naturalHeight, 1); clampPan(); const ratio = window.devicePixelRatio || 1; canvas.value.style.width = `${displayWidth}px`; canvas.value.style.height = `${displayHeight}px`; canvas.value.width = Math.round(displayWidth * ratio); canvas.value.height = Math.round(displayHeight * ratio); draw() }
function rawPoint(event: PointerEvent) { const bounds = canvas.value!.getBoundingClientRect(); return { x: event.clientX - bounds.left, y: event.clientY - bounds.top } }
function pointFromRaw(point: { x: number; y: number }) { const box = imageBox(); return { x: clamp((point.x - box.x) / box.width, 0, 1), y: clamp((point.y - box.y) / box.height, 0, 1) } }
function hitTest(point: { x: number; y: number }) { const value = region.value; const box = imageBox(); const thresholdX = Math.min(.06, 18 / Math.max(box.width, 1)); const thresholdY = Math.min(.06, 18 / Math.max(box.height, 1)); const left = Math.abs(point.x - value.x) <= thresholdX; const right = Math.abs(point.x - (value.x + value.width)) <= thresholdX; const top = Math.abs(point.y - value.y) <= thresholdY; const bottom = Math.abs(point.y - (value.y + value.height)) <= thresholdY; const withinX = point.x >= value.x - thresholdX && point.x <= value.x + value.width + thresholdX; const withinY = point.y >= value.y - thresholdY && point.y <= value.y + value.height + thresholdY; if (left && top) return 'nw'; if (right && top) return 'ne'; if (left && bottom) return 'sw'; if (right && bottom) return 'se'; if (top && withinX) return 'n'; if (bottom && withinX) return 's'; if (left && withinY) return 'w'; if (right && withinY) return 'e'; if (point.x >= value.x && point.x <= value.x + value.width && point.y >= value.y && point.y <= value.y + value.height) return 'move'; return 'new' }
function cursorForMode(mode: string) { return ({ nw: 'nwse-resize', se: 'nwse-resize', ne: 'nesw-resize', sw: 'nesw-resize', n: 'ns-resize', s: 'ns-resize', w: 'ew-resize', e: 'ew-resize', move: 'move', new: 'crosshair', pan: 'grab' } as Record<string, string>)[mode] ?? 'crosshair' }
function pinchDistanceNow() { const values = [...pointers.values()]; return Math.hypot(values[0].x - values[1].x, values[0].y - values[1].y) }
function pinchMidpointNow() { const values = [...pointers.values()]; return { x: (values[0].x + values[1].x) / 2, y: (values[0].y + values[1].y) / 2 } }
function keepPointAnchored(point: { x: number; y: number }, nextZoom: number) { const before = imageBox(); const relativeX = (point.x - before.x) / before.width; const relativeY = (point.y - before.y) / before.height; zoom.value = clamp(nextZoom, 1, 4); const after = imageBox(); pan.x += point.x - (after.x + relativeX * after.width); pan.y += point.y - (after.y + relativeY * after.height); clampPan() }
function changeZoom(amount: number) { keepPointAnchored({ x: displayWidth / 2, y: displayHeight / 2 }, zoom.value + amount); draw(); canvas.value?.focus() }
function onPointerDown(event: PointerEvent) { if (!canvas.value || !isReady.value) return; const raw = rawPoint(event); pointers.set(event.pointerId, raw); canvas.value.setPointerCapture(event.pointerId); if (pointers.size === 2) { pinchDistance = pinchDistanceNow(); activePointer = null; dragMode = 'pinch'; return }; activePointer = event.pointerId; dragRawOrigin = raw; dragOrigin = pointFromRaw(raw); dragStart = { ...region.value }; panStart = { ...pan }; dragMode = isPanning.value ? 'pan' : hitTest(dragOrigin); if (dragMode === 'new') region.value = { x: dragOrigin.x, y: dragOrigin.y, width: .03, height: .03 }; canvas.value.style.cursor = cursorForMode(dragMode); draw() }
function onPointerMove(event: PointerEvent) { if (!canvas.value || !isReady.value) return; const raw = rawPoint(event); if (pointers.has(event.pointerId)) pointers.set(event.pointerId, raw); if (pointers.size >= 2) { const midpoint = pinchMidpointNow(); const nextDistance = pinchDistanceNow(); if (pinchDistance > 0) keepPointAnchored(midpoint, zoom.value * nextDistance / pinchDistance); pinchDistance = nextDistance; draw(); return }; const point = pointFromRaw(raw); if (activePointer !== event.pointerId) { canvas.value.style.cursor = cursorForMode(isPanning.value ? 'pan' : hitTest(point)); return }; if (dragMode === 'pan') { pan = { x: panStart.x + raw.x - dragRawOrigin.x, y: panStart.y + raw.y - dragRawOrigin.y }; clampPan(); draw(); return }; const minimum = .03; const deltaX = point.x - dragOrigin.x; const deltaY = point.y - dragOrigin.y; if (dragMode === 'new') { const x = Math.min(dragOrigin.x, point.x); const y = Math.min(dragOrigin.y, point.y); region.value = { x, y, width: Math.max(minimum, Math.abs(point.x - dragOrigin.x)), height: Math.max(minimum, Math.abs(point.y - dragOrigin.y)) } } else if (dragMode === 'move') region.value = { ...dragStart, x: clamp(dragStart.x + deltaX, 0, 1 - dragStart.width), y: clamp(dragStart.y + deltaY, 0, 1 - dragStart.height) }; else { let left = dragStart.x; let top = dragStart.y; let right = dragStart.x + dragStart.width; let bottom = dragStart.y + dragStart.height; if (dragMode.includes('w')) left = clamp(dragStart.x + deltaX, 0, right - minimum); if (dragMode.includes('e')) right = clamp(right + deltaX, left + minimum, 1); if (dragMode.includes('n')) top = clamp(dragStart.y + deltaY, 0, bottom - minimum); if (dragMode.includes('s')) bottom = clamp(bottom + deltaY, top + minimum, 1); region.value = { x: left, y: top, width: right - left, height: bottom - top } }; region.value.width = Math.min(region.value.width, 1 - region.value.x); region.value.height = Math.min(region.value.height, 1 - region.value.y); draw() }
function onPointerUp(event: PointerEvent) { pointers.delete(event.pointerId); if (canvas.value?.hasPointerCapture(event.pointerId)) canvas.value.releasePointerCapture(event.pointerId); activePointer = null; dragMode = ''; draw() }
function onCanvasKeydown(event: KeyboardEvent) { if (!['ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown'].includes(event.key)) return; event.preventDefault(); const step = event.ctrlKey ? .02 : .005; const value = { ...region.value }; if (event.shiftKey) { if (event.key === 'ArrowLeft') value.width = Math.max(.03, value.width - step); if (event.key === 'ArrowRight') value.width = Math.min(1 - value.x, value.width + step); if (event.key === 'ArrowUp') value.height = Math.max(.03, value.height - step); if (event.key === 'ArrowDown') value.height = Math.min(1 - value.y, value.height + step) } else { if (event.key === 'ArrowLeft') value.x = Math.max(0, value.x - step); if (event.key === 'ArrowRight') value.x = Math.min(1 - value.width, value.x + step); if (event.key === 'ArrowUp') value.y = Math.max(0, value.y - step); if (event.key === 'ArrowDown') value.y = Math.min(1 - value.height, value.y + step) }; region.value = value; draw() }
function useWholeImage() { region.value = { x: 0, y: 0, width: 1, height: 1 }; draw(); canvas.value?.focus() }
function confirmCrop() { emit('confirm', isWholeImage(region.value) ? null : { ...region.value }) }
function onWindowKeydown(event: KeyboardEvent) { if (event.key === 'Escape') { emit('cancel'); return }; if (event.key !== 'Tab' || !dialog.value) return; const focusable = Array.from(dialog.value.querySelectorAll<HTMLElement>('button:not(:disabled), canvas[tabindex="0"]')); if (!focusable.length) return; const first = focusable[0]; const last = focusable[focusable.length - 1]; if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus() } else if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus() } }
onMounted(() => { previousBodyOverflow = document.body.style.overflow; document.body.style.overflow = 'hidden'; objectUrl = URL.createObjectURL(props.file); image = new Image(); image.onload = async () => { isReady.value = true; await nextTick(); resizeCanvas(); canvas.value?.focus() }; image.onerror = () => { loadError.value = '无法预览这张图片，请改用 JPG、PNG 或 WebP。' }; image.src = objectUrl; window.addEventListener('resize', resizeCanvas); window.addEventListener('keydown', onWindowKeydown) })
onBeforeUnmount(() => { document.body.style.overflow = previousBodyOverflow; window.removeEventListener('resize', resizeCanvas); window.removeEventListener('keydown', onWindowKeydown); if (objectUrl) URL.revokeObjectURL(objectUrl) })
</script>

<template>
  <Teleport to="body"><div class="crop-overlay" @click.self="emit('cancel')"><section ref="dialog" class="crop-dialog" role="dialog" aria-modal="true" aria-labelledby="crop-title" aria-describedby="crop-help"><header class="crop-header"><div><p>识别范围</p><h2 id="crop-title">截取题目区域</h2></div><button type="button" aria-label="关闭裁剪" @click="emit('cancel')"><X :size="21" /></button></header><p id="crop-help" class="crop-help">拖动橙色框或控制点选择题目。手机可双指缩放、双指拖动画面；也可用下方按钮放大后开启“移动图片”。</p><div ref="frame" class="crop-frame"><canvas v-show="isReady" ref="canvas" class="crop-canvas" tabindex="0" role="application" :aria-label="`图片裁剪区域，${cropSummary}`" @pointerdown="onPointerDown" @pointermove="onPointerMove" @pointerup="onPointerUp" @pointercancel="onPointerUp" @keydown="onCanvasKeydown"></canvas><p v-if="!isReady && !loadError" class="crop-loading">正在准备图片…</p><p v-if="loadError" class="crop-error" role="alert">{{ loadError }}</p></div><div v-if="isReady" class="crop-view-tools" aria-label="图片查看工具"><button type="button" aria-label="缩小图片" :disabled="zoom <= 1" @click="changeZoom(-.25)"><ZoomOut :size="18" /></button><output>{{ Math.round(zoom * 100) }}%</output><button type="button" aria-label="放大图片" :disabled="zoom >= 4" @click="changeZoom(.25)"><ZoomIn :size="18" /></button><button type="button" class="pan-toggle" :class="{ active: isPanning }" :aria-pressed="isPanning" @click="isPanning = !isPanning"><Hand :size="17" />{{ isPanning ? '正在移动图片' : '移动图片' }}</button></div><div class="crop-meta"><strong>{{ props.file.name }}</strong><span aria-live="polite">{{ cropSummary }}</span></div><footer class="crop-actions"><button class="whole-button" type="button" :disabled="!isReady" @click="useWholeImage"><Maximize2 :size="17" />使用整张图</button><div><button class="cancel-button" type="button" @click="emit('cancel')">取消</button><button class="confirm-button" type="button" :disabled="!isReady" @click="confirmCrop"><Check :size="17" />确认范围</button></div></footer></section></div></Teleport>
</template>

<style scoped>
.crop-overlay{position:fixed;z-index:1000;inset:0;display:grid;place-items:center;padding:24px;background:rgba(15,35,57,.58)}.crop-dialog{display:flex;flex-direction:column;width:min(960px,100%);max-height:calc(100dvh - 48px);padding:22px;overflow:auto;border-radius:16px;background:#fff;box-shadow:0 20px 45px rgba(15,35,57,.22)}.crop-header{display:flex;align-items:flex-start;justify-content:space-between;gap:16px}.crop-header p{margin:0 0 4px;color:#71879e;font-size:12px;font-weight:700}.crop-header h2{margin:0;color:#203954;font-size:21px}.crop-header button{display:grid;width:44px;height:44px;flex:0 0 auto;place-items:center;color:#60768e;border:0;border-radius:9px;background:#f1f5f9;cursor:pointer}.crop-help{margin:9px 52px 15px 0;color:#60758c;font-size:13px;line-height:1.55}.crop-frame{display:grid;min-height:260px;flex:1;place-items:center;overflow:hidden;padding:12px;border-radius:12px;background:#172534}.crop-canvas{display:block;border-radius:4px;outline:0;touch-action:none;user-select:none}.crop-canvas:focus-visible{box-shadow:0 0 0 4px rgba(96,165,250,.75)}.crop-loading,.crop-error{color:#dce8f3;font-size:14px}.crop-error{color:#fecaca}.crop-view-tools{display:flex;min-height:44px;align-items:center;gap:7px;margin-top:10px}.crop-view-tools button{display:grid;width:44px;height:44px;place-items:center;color:#315f9b;border:1px solid #bed0e5;border-radius:8px;background:#fff;cursor:pointer}.crop-view-tools button:disabled{cursor:not-allowed;opacity:.45}.crop-view-tools output{display:grid;min-width:48px;place-items:center;color:#526e89;font-size:12px;font-weight:700}.crop-view-tools .pan-toggle{display:flex;width:auto;gap:6px;padding:0 11px;margin-left:auto;font-size:12px;font-weight:700}.crop-view-tools .pan-toggle.active{color:#fff;border-color:#2563eb;background:#2563eb}.crop-meta{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:11px;color:#71849a;font-size:11px}.crop-meta strong{overflow:hidden;color:#405a75;text-overflow:ellipsis;white-space:nowrap}.crop-actions{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:17px}.crop-actions>div{display:flex;gap:9px}.crop-actions button{display:inline-flex;min-height:44px;align-items:center;justify-content:center;gap:6px;padding:9px 14px;border-radius:8px;font-size:13px;font-weight:700;cursor:pointer;transition:border-color .18s ease,background .18s ease,opacity .18s ease}.crop-actions button:focus-visible,.crop-header button:focus-visible,.crop-view-tools button:focus-visible{outline:3px solid rgba(37,99,235,.28);outline-offset:2px}.whole-button,.cancel-button{color:#315f9b;border:1px solid #bed0e5;background:#fff}.whole-button:hover,.cancel-button:hover{background:#f3f7fc}.confirm-button{min-width:126px;color:#fff;border:1px solid #f97316;background:#f97316}.confirm-button:hover{background:#dc5f0b}.crop-actions button:disabled{cursor:not-allowed;opacity:.45}@media(max-width:680px){.crop-overlay{align-items:end;padding:0}.crop-dialog{width:100%;max-height:100dvh;padding:16px;padding-bottom:calc(16px + env(safe-area-inset-bottom));border-radius:16px 16px 0 0}.crop-header h2{font-size:clamp(21px,6vw,27px)}.crop-help{margin-right:0;font-size:12px}.crop-frame{height:min(42dvh,390px);min-height:220px;flex:0 0 auto;padding:6px}.crop-meta{align-items:flex-start;flex-direction:column;gap:4px}.crop-actions{align-items:stretch;flex-direction:column-reverse}.crop-actions>div{display:grid;grid-template-columns:1fr 1.35fr}.whole-button{width:100%}.crop-view-tools{flex-wrap:wrap}.crop-view-tools .pan-toggle{margin-left:0;flex:1;justify-content:center}}@media(prefers-reduced-motion:reduce){.crop-actions button{transition:none}}
</style>
